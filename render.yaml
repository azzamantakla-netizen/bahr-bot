services:
  - type: web
    name: texas4win-bot
    runtime: python
    buildCommand: pip install -r requirements.txt && playwright install chromium
    startCommand: python bot.py
    envVars:
      - key: PORT
        value: 10000
